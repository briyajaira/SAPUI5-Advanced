<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns="sap.m">

    <Panel id="panelTable" >
        <Button id="btnShowCity" text="{i18n>showCity}" visible="{jsonModelConfig>/visibleBtnShowCity}" press="onShowCity" />
        <Button id="btnHideCity" text="{i18n>hideCity}" visible="{jsonModelConfig>/visibleBtnHideCity}" press="onHideCity" />     
 
            <Table id="tableEmployee" items="{odataNorthwind>/Employees}">
                <columns>
                    <Column id="colId" visible="{jsonModelConfig>/visibleID}">
                         <header>
                            <Label id="labelId" text="{i18n>tableId}"/>
                         </header>
                     </Column>
                           <Column id="colName" visible="{jsonModelConfig>/visibleName}">
                                <header>
                                    <Label id="labelName" text="{i18n>name}"/>
                                </header>
                            </Column>
                            <Column id="colCountry" visible="{jsonModelConfig>/visibleCountry}">
                                <header>
                                    <Label id="labelCount" text="{i18n>country}"/>
                                 </header>
                            </Column>
                            <Column id="colCity" visible="{jsonModelConfig>/visibleCity}">
                                <header>
                                    <Label id="labelCity" text="{i18n>city}"/>
                                </header>
                            </Column>        
                            <Column id="colShow" width="100px">
                                <header>
                                    <Label id="labelShow" text="{i18n>tableShowDetails}"/>
                                </header>
                            </Column>                               
                             </columns>
                                <items>
                                    <ColumnListItem id="colL" type="Navigation" press="showEmployee">
                                        <cells>
                                            <Label id="labelCid" text="{odataNorthwind>EmployeeID}"/>
                                            <Label id="labelCNam" text="{odataNorthwind>LastName}, {odataNorthwind>FirstName}"/>
                                            <Label id="labelCCount" text="{odataNorthwind>Country}"/>
                                            <Label id="labelCCity" text="{odataNorthwind>City}"/>
                                            <core:Icon id="iconShow" src="sap-icon://show"  press="showOrders"/>
                                        </cells>
                                    </ColumnListItem>
                                </items>
             </Table>
             <HBox id="ordersTable"></HBox>
    </Panel>

</core:FragmentDefinition>